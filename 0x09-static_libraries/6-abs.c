#include "main.h"
/**
 *_abs - Takes the absolute of numbers
 *Return: number after success
 *@n: number to be taken the absolute of
 */
int _abs(int n)
{
if (n < 0)
return (n * -1);
else
return (n);
}
