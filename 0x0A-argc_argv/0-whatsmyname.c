#include <stdio.h>
/**
 *main - the entry point of the program
 *@argv : the arg vector
 *@argc : the arg counter
 *Return: 0 on success
 */
int main(int argc, char *argv[])
{
printf("%s", argv[0]);
return (0);
}
